using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class code_tests : MonoBehaviour
{
    public GameObject myObject;
    // Start is called before the first frame update
    void Start()
    {
        Queue myQ = new Queue();
        myQ.Enqueue("Hello");
        myQ.Enqueue("World");
        myQ.Enqueue("!");

        //PrintValues(myQ);
        
        for(int i=0; i<10; i++){
            Instantiate(myObject, new Vector3(0.0f, 0.0f, 0.0f), Quaternion.identity);
        }

        Debug.Log(myObject.num_objects_created_from_this_class);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public static void PrintValues(IEnumerable myCollection){
        //foreach(SystemObject obj in myCollection) {
            //Debug.Log(obj);
        //}
    //}
}
