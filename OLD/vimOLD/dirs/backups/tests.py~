def performZeroingRoutine():
    #while not limitSwitchPressed
        #runServo1 in reverse position
        #(once pressed) run forward for half a turn
    


#loop
while not terminate:
    #do functions

    #continue?
    response = input("Ask user a question? (y/n): ')
            if response == 'n':
                #terminate
                terminate = True
